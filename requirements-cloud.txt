# Cloud-mode dependencies (GitHub Actions + Turso).
# Note: PySide6 is NOT needed for the scrape job itself — only for the
# desktop client.  Keeping it out shaves ~150 MB off the CI install.

pandas>=2.0.0
requests>=2.28.0
beautifulsoup4>=4.12.0
cloudscraper>=1.2.0
libsql-client>=0.3.1
